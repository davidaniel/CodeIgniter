   
# GitHub Action for CodeIgniter
name: Testing CodeIgniter
on: [push, pull_request]
jobs:
  build:
    strategy:
      matrix:
        operating-system: [ubuntu-latest]
        php-versions: ['7.3']
#        php-versions: ['7.2', '7.3', '7.4']
    runs-on: ${{ matrix.operating-system }}
    steps:
      - name: Checkout
        uses: actions/checkout@v2

      - name: Setup PHP, with composer and extensions
        uses: shivammathur/setup-php@v2 #https://github.com/shivammathur/setup-php
        with:
          php-version: ${{ matrix.php-versions }}
          extensions: mbstring, intl, curl
          #coverage: xdebug #optional
          tools: cs2pr, phpstan, psalm, phpcs, phpcbf, phpmd, php-cs-fixer

      - name: Get composer cache directory
        id: composer-cache
        run: echo "::set-output name=dir::$(composer config cache-files-dir)"

      - name: Cache composer dependencies
        uses: actions/cache@v2
        with:
          path: ${{ steps.composer-cache.outputs.dir }}
          # Use composer.json for key, if composer.lock is not committed.
          # key: ${{ runner.os }}-composer-${{ hashFiles('**/composer.json') }}
          key: ${{ runner.os }}-composer-${{ hashFiles('**/composer.lock') }}
          restore-keys: ${{ runner.os }}-composer-

      - name: Install dependencies
        run: composer install --no-progress --prefer-dist --optimize-autoloader

#      - name: Run PHPStan
#        run: phpstan analyse .

#      - name: Run Psalm
#        run: psalm --output-format=checkstyle | cs2pr

      # PHP_CodeSniffer
      #funciona, demasiados erros de estilo
#      - name: Run phpcs
#        run: phpcs --standard=PSR2 ./application | cs2pr

      - name : Run PHPMD
        run: phpmd ./application github .github/phpmd-rules.xml --exclude 'application/vendor,application/third_party'

#      - name: "Run friendsofphp/php-cs-fixer"
#        run: "vendor/bin/php-cs-fixer fix --diff --dry-run --format=checkstyle | cs2pr"
